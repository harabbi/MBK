CREATE DATABASE IF NOT EXISTS `magento`;
CREATE TABLE if not exists `m_products` (
  `sku`                text,
  `_store`             text,
  `_attribute_set`     text,
  `_type`              text,
  `_category`          text,
  `_product_websites`  text,
  `name`               text,
  `description`        text,
  `short_description`  text,
  `price`              text,
  `special_price`      text,
  `special_from_date`  text,
  `special_to_date`    text,
  `cost`               text,
  `weight`             text,
  `manufacturer`       text,
  `meta_title`         text,
  `meta_keyword`       text,
  `meta_description`   text,
  `image`              text,
  `small_image`        text,
  `thumbnail`          text,
  `media_gallery`      text,
  `color`              text,
  `news_from_date`     text,
  `news_to_date`       text,
  `gallery`            text,
  `status`             text,
  `tax_class_id`       text,
  `url_key`            text,
  `url_path`           text,
  `minimal_price`      text,
  `visibility`         text,
  `custom_design`      text,
  `custom_design_from` text,
  `custom_design_to`     text,
  `custom_layout_update` text,
  `page_layout`         text,
  `options_container`   text,
  `required_options`    text,
  `has_options`         text,
  `image_label`         text,
  `small_image_label`   text,
  `thumbnail_label` text,
  `created_at` text,
  `updated_at` text,
  `enable_googlecheckout` text,
  `gift_message_available` text,
  `gift_wrapping_available` text,
  `gift_wrapping_price` text,
  `related_targetrule_position_limit` text,
  `related_targetrule_position_behavior` text,
  `upsell_targetrule_position_limit` text,
  `upsell_targetrule_position_behavior` text,
  `is_imported` text,
  `features` text,
  `specifications` text,
  `retail_price` text,
  `product_video` text,
  `product_code` text,
  `newest` text,
	`mbk_book_subject` text,
	`mbk_dcc_and_sound` text,
	`mbk_features_area` text,
	`mbk_freightcar_type` text,
	`mbk_locomotive_type` text,
	`mbk_magazine_publisher` text,
	`mbk_map_price` text,
	`mbk_passengercar_type` text,
	`mbk_pre_weathered` text,
	`mbk_product_code` text,
	`mbk_retail_price` text,
	`mbk_roadname` text,
	`mbk_structure_type` text,
	`mbk_vehicle_type` text,
	`mbk_video` text,
  `map_price` text,
  `qty` text,
  `min_qty` text,
  `use_config_min_qty` text,
  `is_qty_decimal` text,
  `backorders` text,
  `use_config_backorders` text,
  `min_sale_qty` text,
  `use_config_min_sale_qty` text,
  `max_sale_qty` text,
  `use_config_max_sale_qty` text,
  `is_in_stock` text,
  `notify_stock_qty` text,
  `use_config_notify_stock_qty` text,
  `manage_stock` text,
  `use_config_manage_stock` text,
  `use_config_qty_increments` text,
  `qty_increments` text,
  `use_config_enable_qty_increments` text,
  `enable_qty_increments` text,
  `_links_related_sku` text,
  `_links_related_position` text,
  `_links_crosssell_sku` text,
  `_links_crosssell_position` text,
  `_links_upsell_sku` text,
  `_links_upsell_position` text,
  `_associated_sku` text,
  `_associated_default_qty` text,
  `_associated_position` text,
  `_tier_price_website` text,
  `_tier_price_customer_group` text,
  `_tier_price_qty` text,
  `_tier_price_price` text,
  `mbk_import_update`     tinyint(1) DEFAULT '0',
  `mbk_import_new`        tinyint(1) DEFAULT '0',
  `mbk_ready_to_import`   tinyint(1) DEFAULT '0',
  `mbk_updated_at`        timestamp NOT NULL DEFAULT '0000-00-00 00:00:00' ON UPDATE CURRENT_TIMESTAMP,
  `mbk_created_at`        datetime DEFAULT NULL
) ENGINE=MyISAM DEFAULT CHARSET=latin1;
